app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/mesto
      username: postgres
      password: 'abc123456'
      pool-size: 30

server:
  error:
    include-binding-errors: always
    include-message: always


